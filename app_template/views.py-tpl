from django.shortcuts import render

from .models import Eintrag


def index(request):
    return render(
        request,
        "{{ app_name }}/index.html",
        {"eintraege": Eintrag.objects.all()},
    )
