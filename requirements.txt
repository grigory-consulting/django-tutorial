# Direkte Projekt-Abhaengigkeiten (Versionen aus der Seminar-.venv).
# Transitive Pakete zieht pip automatisch nach.

Django==5.2.15
djangorestframework==3.17.1
channels==4.3.2          # WebSockets / Realtime-Lab
daphne==4.2.2            # ASGI-Server (ersetzt runserver)
whitenoise==6.12.0       # statische Dateien ohne separaten Webserver
requests==2.34.2         # Datenimport / fetch

# Produktion (optional):
gunicorn==26.0.0

# Nur fuer die Lab-Notebooks (optional):
ipykernel==7.2.0
